<% @posts.each do |post| %>
  <div><!--<p><%= @post.updated_at.strftime("Last updated %A, %b, %e, at %l:%M %p") %></p>

this is from the posts_helpers.rb
-->
<%= post.title %> - <%= last_updated post %>
  </div>
<% end %>